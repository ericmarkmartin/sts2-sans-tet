[project]
name = "sts2-rl"
version = "0.1.0"
description = "RL training harness for Slay the Spire 2"
requires-python = ">=3.11"
dependencies = [
    "gymnasium>=1.0.0",
    "numpy>=1.26.0",
]

[project.optional-dependencies]
train = [
    "stable-baselines3>=2.3.0",
    "tensorboard>=2.15.0",
]
